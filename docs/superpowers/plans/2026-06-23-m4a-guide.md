# M4a · 玩法介绍 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现玩法说明/奖级规则/活动公告的资讯展示：PlayGuide 模型 + 只读接口 + Django Admin 内容管理 + 示例数据 + H5"玩法"页。

**Architecture:** 新建 `guide` app(PlayGuide 模型 + openapi 只读接口 + Admin)，前端加 api/guide.js 与 pages/guide/{index,detail}，tabBar 加"玩法"。content 存 HTML，H5 用 `<rich-text>` 渲染。复用 make_response/LotteryTabs/lotteryStore/request。

**Tech Stack:** Django 5.2 + DRF；uni-app(Vue3+Vite, JS) + vitest。

## Global Constraints

- 玩法/奖级/活动是**资讯陈列**，content 与标题**不得出现"预测/推荐"**(合规红线)；活动不导向售彩/代购。
- 返回统一 `common.utils.make_response`(成功 code=0，应用级错误 code=1 且 HTTP 200)。
- 后端日志用 logging 禁止 print；异常 exc_info=True。
- 对外只返回 `is_active=True` 且(publish_at 为空 或 ≤ now)的条目；时间用 `django.utils.timezone.now()`。
- 前端 uni-app(Vue3+Vite) JS，不引 Pinia/Vuex；request code=0 resolve body.data；content 用 `<rich-text>` 渲染。
- type 取值：`intro`(玩法说明)/`rule`(奖级规则)/`activity`(活动公告)；type_label 由 serializer 的 get_type_display 提供。
- detail 的 id 参数先 int() 兜底，非整数 → code=1(避免 500)。
- 前端命令在 `lottery_frontend/`；后端命令在 `lottery_backend/`(先激活 .venv)。后端测试 DB Docker lottery_pg 127.0.0.1:5433。端口：后端 8123、前端 5199。

---

## File Structure

- `lottery_backend/guide/__init__.py`、`apps.py`、`models.py`(PlayGuide)、`admin.py`、`serializers.py`、`views.py`、`urls.py`(新建 app)
- `lottery_backend/guide/migrations/`(生成)
- `lottery_backend/guide/management/commands/seed_sample_guides.py`(新)
- `lottery_backend/guide/tests/`(新)：模型/接口/seed 测试
- `lottery_backend/config/settings.py`(改)：INSTALLED_APPS 加 `guide`
- `lottery_backend/config/urls.py`(改)：加 `api/openapi/guide/` include
- `lottery_frontend/src/api/guide.js`(新)
- `lottery_frontend/src/pages/guide/{index,detail}.vue`(新)
- `lottery_frontend/src/pages.json`(改)：注册 guide 两页 + tabBar"玩法"
- `lottery_frontend/tests/guide.test.js`(新)

---

### Task 1: PlayGuide 模型 + guide app + Admin

**Files:**
- Create: `lottery_backend/guide/__init__.py`(空)、`guide/apps.py`、`guide/models.py`、`guide/admin.py`
- Create: `lottery_backend/guide/migrations/__init__.py`(空)、`guide/tests/__init__.py`(空)
- Create: `lottery_backend/guide/tests/test_models.py`
- Modify: `lottery_backend/config/settings.py`(INSTALLED_APPS 加 guide)

**Interfaces:**
- Consumes: `lottery.models.Lottery`
- Produces: `guide.models.PlayGuide`，字段 `lottery(FK null, related_name="guides")/type/title/content/sort/is_active/publish_at/created_at/updated_at`；常量 `TYPE_INTRO="intro"`/`TYPE_RULE="rule"`/`TYPE_ACTIVITY="activity"`；`PlayGuideAdmin`。

- [ ] **Step 1: 建 app 骨架 + 注册**

`lottery_backend/guide/apps.py`:
```python
from django.apps import AppConfig


class GuideConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "guide"
```

`lottery_backend/config/settings.py` — INSTALLED_APPS 在 `"usernumber",` 后加一行：
```python
    "usernumber",
    "guide",
]
```

`guide/__init__.py`、`guide/migrations/__init__.py`、`guide/tests/__init__.py` 均为空文件。

- [ ] **Step 2: 写失败测试**

`lottery_backend/guide/tests/test_models.py`:
```python
import pytest
from guide.models import PlayGuide


def test_create_defaults(db):
    g = PlayGuide.objects.create(title="双色球玩法", type=PlayGuide.TYPE_INTRO)
    assert g.lottery is None
    assert g.is_active is True
    assert g.sort == 0
    assert g.publish_at is None
    assert g.content == ""


def test_type_constants():
    assert PlayGuide.TYPE_INTRO == "intro"
    assert PlayGuide.TYPE_RULE == "rule"
    assert PlayGuide.TYPE_ACTIVITY == "activity"
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest guide/tests/test_models.py -v`
Expected: FAIL（`ModuleNotFoundError: guide.models`）

- [ ] **Step 4: 写模型**

`lottery_backend/guide/models.py`:
```python
from django.db import models
from lottery.models import Lottery


class PlayGuide(models.Model):
    """玩法说明 / 奖级规则 / 活动公告。lottery 为空 = 通用。"""
    TYPE_INTRO = "intro"
    TYPE_RULE = "rule"
    TYPE_ACTIVITY = "activity"
    TYPE_CHOICES = [(TYPE_INTRO, "玩法说明"), (TYPE_RULE, "奖级规则"), (TYPE_ACTIVITY, "活动公告")]

    lottery = models.ForeignKey(Lottery, on_delete=models.CASCADE, null=True, blank=True,
                                related_name="guides", verbose_name="彩种")
    type = models.CharField("类型", max_length=12, choices=TYPE_CHOICES, default=TYPE_INTRO)
    title = models.CharField("标题", max_length=100)
    content = models.TextField("内容", blank=True, default="")
    sort = models.IntegerField("排序", default=0)
    is_active = models.BooleanField("是否上架", default=True)
    publish_at = models.DateTimeField("发布时间", null=True, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        verbose_name = verbose_name_plural = "玩法介绍"
        ordering = ["sort", "-publish_at"]

    def __str__(self):
        return f"{self.get_type_display()} {self.title}"
```

- [ ] **Step 5: 写 Admin**

`lottery_backend/guide/admin.py`:
```python
from django.contrib import admin
from guide.models import PlayGuide


@admin.register(PlayGuide)
class PlayGuideAdmin(admin.ModelAdmin):
    list_display = ("title", "type", "lottery", "is_active", "sort", "publish_at")
    list_filter = ("type", "is_active", "lottery")
    search_fields = ("title", "content")
    list_editable = ("is_active", "sort")
```

- [ ] **Step 6: 生成迁移**

Run: `python manage.py makemigrations guide`
Expected: 生成 `guide/migrations/0001_initial.py`

- [ ] **Step 7: 跑测试确认通过**

Run: `python -m pytest guide/tests/test_models.py -v`
Expected: PASS（2 passed）

- [ ] **Step 8: 提交**

```bash
git add lottery_backend/guide lottery_backend/config/settings.py
git commit -m "feat: PlayGuide 模型 + guide app + Admin"
```

---

### Task 2: 玩法只读接口 list + detail

**Files:**
- Create: `lottery_backend/guide/serializers.py`、`guide/views.py`、`guide/urls.py`
- Modify: `lottery_backend/config/urls.py`
- Test: `lottery_backend/guide/tests/test_api_guide.py`

**Interfaces:**
- Consumes: `PlayGuide`、`common.utils.make_response`
- Produces:
  - `GET /api/openapi/guide/list?code=&type=` → `{id,type,type_label,title,sort,publish_at}` 列表(不含 content)
  - `GET /api/openapi/guide/detail?id=` → 含 content；非整数/不存在/未发布 → code=1
  - `GuideListSerializer`/`GuideDetailSerializer`，`type_label` 来自 get_type_display

- [ ] **Step 1: 写 serializers**

`lottery_backend/guide/serializers.py`:
```python
from rest_framework import serializers
from guide.models import PlayGuide


class GuideListSerializer(serializers.ModelSerializer):
    type_label = serializers.CharField(source="get_type_display", read_only=True)

    class Meta:
        model = PlayGuide
        fields = ["id", "type", "type_label", "title", "sort", "publish_at"]


class GuideDetailSerializer(serializers.ModelSerializer):
    type_label = serializers.CharField(source="get_type_display", read_only=True)

    class Meta:
        model = PlayGuide
        fields = ["id", "type", "type_label", "title", "content", "sort", "publish_at"]
```

- [ ] **Step 2: 写失败测试**

`lottery_backend/guide/tests/test_api_guide.py`:
```python
import datetime
import pytest
from rest_framework.test import APIClient
from django.utils import timezone
from lottery.models import Lottery
from guide.models import PlayGuide


@pytest.fixture
def ssq(db):
    return Lottery.objects.create(code="ssq", name="双色球", category="福彩",
                                  rule_config={}, draw_days=[2, 4, 7])


def test_list_code_includes_common(ssq):
    PlayGuide.objects.create(lottery=ssq, type="intro", title="双色球玩法")
    PlayGuide.objects.create(lottery=None, type="activity", title="通用活动")
    dlt = Lottery.objects.create(code="dlt", name="大乐透", category="体彩",
                                 rule_config={}, draw_days=[1, 3, 6])
    PlayGuide.objects.create(lottery=dlt, type="intro", title="大乐透玩法")
    titles = [g["title"] for g in APIClient().get("/api/openapi/guide/list?code=ssq").json()["data"]]
    assert "双色球玩法" in titles
    assert "通用活动" in titles
    assert "大乐透玩法" not in titles


def test_list_type_filter(ssq):
    PlayGuide.objects.create(lottery=ssq, type="intro", title="玩法")
    PlayGuide.objects.create(lottery=ssq, type="rule", title="奖级")
    titles = [g["title"] for g in APIClient().get("/api/openapi/guide/list?code=ssq&type=rule").json()["data"]]
    assert titles == ["奖级"]


def test_list_excludes_inactive_and_future(ssq):
    PlayGuide.objects.create(lottery=ssq, type="intro", title="上架")
    PlayGuide.objects.create(lottery=ssq, type="intro", title="下架", is_active=False)
    PlayGuide.objects.create(lottery=ssq, type="intro", title="未来",
                             publish_at=timezone.now() + datetime.timedelta(days=1))
    titles = [g["title"] for g in APIClient().get("/api/openapi/guide/list?code=ssq").json()["data"]]
    assert "上架" in titles
    assert "下架" not in titles
    assert "未来" not in titles


def test_list_no_content_field(ssq):
    PlayGuide.objects.create(lottery=ssq, type="intro", title="玩法", content="<p>hi</p>")
    data = APIClient().get("/api/openapi/guide/list?code=ssq").json()["data"]
    assert "content" not in data[0]
    assert data[0]["type_label"] == "玩法说明"


def test_detail_returns_content(ssq):
    g = PlayGuide.objects.create(lottery=ssq, type="intro", title="玩法", content="<p>规则</p>")
    body = APIClient().get(f"/api/openapi/guide/detail?id={g.id}").json()
    assert body["code"] == 0
    assert body["data"]["content"] == "<p>规则</p>"


def test_detail_missing(ssq):
    assert APIClient().get("/api/openapi/guide/detail?id=99999").json()["code"] == 1


def test_detail_invalid_id(ssq):
    assert APIClient().get("/api/openapi/guide/detail?id=abc").json()["code"] == 1
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest guide/tests/test_api_guide.py -v`
Expected: FAIL（404 / 视图与路由未定义）

- [ ] **Step 4: 写视图**

`lottery_backend/guide/views.py`:
```python
import logging

from django.db.models import Q
from django.utils import timezone
from rest_framework.views import APIView
from rest_framework.response import Response

from common.utils import make_response
from guide.models import PlayGuide
from guide.serializers import GuideListSerializer, GuideDetailSerializer

logger = logging.getLogger(__name__)


def _visible_qs():
    """上架且已发布(publish_at 为空或已过)的条目。"""
    now = timezone.now()
    return (PlayGuide.objects.filter(is_active=True)
            .filter(Q(publish_at__isnull=True) | Q(publish_at__lte=now)))


class GuideListView(APIView):
    """GET /api/openapi/guide/list?code=&type= —— 玩法/奖级/活动列表(不含 content)。"""

    def get(self, request):
        qs = _visible_qs()
        code = request.query_params.get("code")
        if code:
            qs = qs.filter(Q(lottery__code=code) | Q(lottery__isnull=True))
        gtype = request.query_params.get("type")
        if gtype:
            qs = qs.filter(type=gtype)
        return Response(make_response(data=GuideListSerializer(qs, many=True).data))


class GuideDetailView(APIView):
    """GET /api/openapi/guide/detail?id= —— 单条详情(含 content)。"""

    def get(self, request):
        raw_id = request.query_params.get("id")
        try:
            gid = int(raw_id)
        except (TypeError, ValueError):
            return Response(make_response(code=1, msg="参数非法", error=f"id={raw_id}"))
        guide = _visible_qs().filter(id=gid).first()
        if guide is None:
            return Response(make_response(code=1, msg="内容不存在或未发布", error=str(raw_id)))
        return Response(make_response(data=GuideDetailSerializer(guide).data))
```

- [ ] **Step 5: 写路由 + 挂载**

`lottery_backend/guide/urls.py`:
```python
from django.urls import path
from guide import views

urlpatterns = [
    path("list", views.GuideListView.as_view(), name="openapi-guide-list"),
    path("detail", views.GuideDetailView.as_view(), name="openapi-guide-detail"),
]
```

`lottery_backend/config/urls.py` — urlpatterns 中，在 `path("api/openapi/", include("lottery.urls")),` **之前**加一行(避免被 lottery include 抢前缀)：
```python
    path("api/openapi/guide/", include("guide.urls")),
    path("api/openapi/", include("lottery.urls")),
```

- [ ] **Step 6: 跑测试确认通过**

Run: `python -m pytest guide/tests/test_api_guide.py -v`
Expected: PASS（7 passed）

- [ ] **Step 7: 提交**

```bash
git add lottery_backend/guide/serializers.py lottery_backend/guide/views.py lottery_backend/guide/urls.py lottery_backend/config/urls.py lottery_backend/guide/tests/test_api_guide.py
git commit -m "feat: 玩法只读接口 guide/list + guide/detail"
```

---

### Task 3: 示例玩法数据命令 seed_sample_guides

**Files:**
- Create: `lottery_backend/guide/management/__init__.py`(空)、`guide/management/commands/__init__.py`(空)
- Create: `lottery_backend/guide/management/commands/seed_sample_guides.py`
- Test: `lottery_backend/guide/tests/test_seed_sample_guides.py`

**Interfaces:**
- Consumes: `Lottery`、`PlayGuide`
- Produces: management command `seed_sample_guides`，幂等(update_or_create 按 title+type)，灌 ssq/dlt 的 intro+rule 各 1 条 + 通用 activity 1 条(共 5)。

- [ ] **Step 1: 写失败测试**

`lottery_backend/guide/tests/test_seed_sample_guides.py`:
```python
import pytest
from django.core.management import call_command
from lottery.models import Lottery
from guide.models import PlayGuide


@pytest.fixture
def seeded(db):
    Lottery.objects.create(code="ssq", name="双色球", category="福彩", rule_config={}, draw_days=[])
    Lottery.objects.create(code="dlt", name="大乐透", category="体彩", rule_config={}, draw_days=[])


def test_seed_creates(seeded):
    call_command("seed_sample_guides")
    assert PlayGuide.objects.filter(is_active=True).count() >= 5
    assert PlayGuide.objects.filter(lottery__isnull=True, type="activity").exists()


def test_seed_idempotent(seeded):
    call_command("seed_sample_guides")
    n = PlayGuide.objects.count()
    call_command("seed_sample_guides")
    assert PlayGuide.objects.count() == n
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest guide/tests/test_seed_sample_guides.py -v`
Expected: FAIL（`Unknown command: 'seed_sample_guides'`）

- [ ] **Step 3: 写命令**

`lottery_backend/guide/management/commands/seed_sample_guides.py`:
```python
import logging
from django.core.management.base import BaseCommand
from lottery.models import Lottery
from guide.models import PlayGuide

logger = logging.getLogger(__name__)

GUIDES = [
    {"code": "ssq", "type": PlayGuide.TYPE_INTRO, "title": "双色球玩法说明", "sort": 1,
     "content": "<p>双色球每注由 6 个红球(1-33)和 1 个蓝球(1-16)组成。</p>"},
    {"code": "ssq", "type": PlayGuide.TYPE_RULE, "title": "双色球奖级规则", "sort": 2,
     "content": "<p>一等奖：6 红 + 1 蓝。</p><p>二等奖：命中 6 红。</p>"},
    {"code": "dlt", "type": PlayGuide.TYPE_INTRO, "title": "大乐透玩法说明", "sort": 1,
     "content": "<p>超级大乐透每注由 5 个前区号(1-35)和 2 个后区号(1-12)组成。</p>"},
    {"code": "dlt", "type": PlayGuide.TYPE_RULE, "title": "大乐透奖级规则", "sort": 2,
     "content": "<p>一等奖：5 前区 + 2 后区。</p>"},
    {"code": None, "type": PlayGuide.TYPE_ACTIVITY, "title": "平台公告", "sort": 1,
     "content": "<p>本平台仅提供开奖查询与号码记录，所有功能免费。</p>"},
]


class Command(BaseCommand):
    help = "插入示例玩法/奖级/活动内容（开发演示用，幂等）"

    def handle(self, *args, **options):
        for g in GUIDES:
            lottery = Lottery.objects.filter(code=g["code"]).first() if g["code"] else None
            obj, created = PlayGuide.objects.update_or_create(
                title=g["title"], type=g["type"],
                defaults={"lottery": lottery, "sort": g["sort"],
                          "content": g["content"], "is_active": True},
            )
            logger.info("seed guide %s %s", g["title"], "created" if created else "updated")
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest guide/tests/test_seed_sample_guides.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
git add lottery_backend/guide/management lottery_backend/guide/tests/test_seed_sample_guides.py
git commit -m "feat: 示例玩法数据命令 seed_sample_guides"
```

---

### Task 4: 前端 guide api

**Files:**
- Create: `lottery_frontend/src/api/guide.js`
- Test: `lottery_frontend/tests/guide.test.js`

**Interfaces:**
- Consumes: `request`(api/request.js)
- Produces: `getGuideList(code, type)`→GET /api/openapi/guide/list (code/type 非空才带)；`getGuideDetail(id)`→GET /api/openapi/guide/detail {id}

- [ ] **Step 1: 写失败测试**

`lottery_frontend/tests/guide.test.js`:
```js
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('../src/api/request.js', () => ({
  request: vi.fn(() => Promise.resolve([])),
}))
import { request } from '../src/api/request.js'
import { getGuideList, getGuideDetail } from '../src/api/guide.js'

describe('guide api', () => {
  beforeEach(() => { request.mockClear() })

  it('list 带 code+type', async () => {
    await getGuideList('ssq', 'rule')
    expect(request).toHaveBeenCalledWith('/api/openapi/guide/list', { data: { code: 'ssq', type: 'rule' } })
  })

  it('list 空 type 不带 type', async () => {
    await getGuideList('ssq', '')
    expect(request).toHaveBeenCalledWith('/api/openapi/guide/list', { data: { code: 'ssq' } })
  })

  it('list 空 code 不带 code', async () => {
    await getGuideList('', '')
    expect(request).toHaveBeenCalledWith('/api/openapi/guide/list', { data: {} })
  })

  it('detail', async () => {
    await getGuideDetail(5)
    expect(request).toHaveBeenCalledWith('/api/openapi/guide/detail', { data: { id: 5 } })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm test`
Expected: FAIL（找不到 guide.js）

- [ ] **Step 3: 写实现**

`lottery_frontend/src/api/guide.js`:
```js
import { request } from './request.js'

export function getGuideList(code, type) {
  const data = {}
  if (code) data.code = code
  if (type) data.type = type
  return request('/api/openapi/guide/list', { data })
}

export function getGuideDetail(id) {
  return request('/api/openapi/guide/detail', { data: { id } })
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm test`
Expected: PASS（guide 全绿）

- [ ] **Step 5: 提交**

```bash
git add lottery_frontend/src/api/guide.js lottery_frontend/tests/guide.test.js
git commit -m "feat: 前端 guide api 函数"
```

---

### Task 5: 玩法页 index + detail + tabBar"玩法"

**Files:**
- Create: `lottery_frontend/src/pages/guide/index.vue`、`guide/detail.vue`
- Modify: `lottery_frontend/src/pages.json`

**Interfaces:**
- Consumes: `getGuideList`/`getGuideDetail`(api/guide)、`getLotteryList`(api/lottery)、`lotteryStore`/`setCode`、`LotteryTabs`
- Produces: 玩法页(彩种切换 + 类型切换 + 标题列表 → 详情)；tabBar 第4项"玩法"。

- [ ] **Step 1: 配 pages.json**

`lottery_frontend/src/pages.json` 整体替换为：
```json
{
  "pages": [
    { "path": "pages/draw/latest", "style": { "navigationBarTitleText": "当期开奖" } },
    { "path": "pages/draw/history", "style": { "navigationBarTitleText": "历史开奖" } },
    { "path": "pages/draw/detail", "style": { "navigationBarTitleText": "开奖详情" } },
    { "path": "pages/draw/stats", "style": { "navigationBarTitleText": "号码统计" } },
    { "path": "pages/guide/index", "style": { "navigationBarTitleText": "玩法介绍" } },
    { "path": "pages/guide/detail", "style": { "navigationBarTitleText": "详情" } },
    { "path": "pages/mine/index", "style": { "navigationBarTitleText": "我的号码" } },
    { "path": "pages/mine/picker", "style": { "navigationBarTitleText": "选号" } }
  ],
  "tabBar": {
    "color": "#666",
    "selectedColor": "#e53935",
    "list": [
      { "pagePath": "pages/draw/latest", "text": "当期" },
      { "pagePath": "pages/draw/history", "text": "历史" },
      { "pagePath": "pages/draw/stats", "text": "统计" },
      { "pagePath": "pages/guide/index", "text": "玩法" },
      { "pagePath": "pages/mine/index", "text": "我的" }
    ]
  },
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "彩票查询",
    "navigationBarBackgroundColor": "#fff",
    "backgroundColor": "#f5f5f5"
  }
}
```

- [ ] **Step 2: 写 index 页**

`lottery_frontend/src/pages/guide/index.vue`:
```vue
<template>
  <view class="page">
    <LotteryTabs :list="lotteries" :active="store.code" @change="onChange" />
    <view class="types">
      <view
        v-for="t in types" :key="t.key"
        class="type" :class="{ active: t.key === curType }"
        @click="chooseType(t.key)"
      >
        <text>{{ t.label }}</text>
      </view>
    </view>
    <view v-for="g in items" :key="g.id" class="row" @click="goDetail(g.id)">
      <text class="title">{{ g.title }}</text>
      <text class="tag">{{ g.type_label }}</text>
    </view>
    <view v-if="!items.length" class="empty">{{ emptyMsg }}</view>
  </view>
</template>

<script setup>
import { ref } from 'vue'
import { onShow } from '@dcloudio/uni-app'
import LotteryTabs from '../../components/LotteryTabs.vue'
import { lotteryStore, setCode } from '../../store/lottery.js'
import { getLotteryList } from '../../api/lottery.js'
import { getGuideList } from '../../api/guide.js'

const store = lotteryStore
const lotteries = ref([])
const items = ref([])
const emptyMsg = ref('加载中…')
const types = [
  { key: '', label: '全部' },
  { key: 'intro', label: '玩法说明' },
  { key: 'rule', label: '奖级规则' },
  { key: 'activity', label: '活动' },
]
const curType = ref('')

async function load() {
  emptyMsg.value = '加载中…'
  try {
    items.value = await getGuideList(store.code, curType.value)
    if (!items.value.length) emptyMsg.value = '暂无内容'
  } catch (e) {
    emptyMsg.value = e.msg || '加载失败'
    uni.showToast({ title: e.msg || '加载失败', icon: 'none' })
  }
}

function onChange(code) { setCode(code); load() }
function chooseType(k) { curType.value = k; load() }
function goDetail(id) { uni.navigateTo({ url: `/pages/guide/detail?id=${id}` }) }

onShow(async () => {
  if (!lotteries.value.length) {
    try { lotteries.value = await getLotteryList() } catch (e) { /* 容错: 彩种拉取失败不阻塞列表 */ }
  }
  load()
})
</script>

<style scoped>
.types { display: flex; padding: 16rpx 20rpx; }
.type { padding: 10rpx 24rpx; margin-right: 14rpx; background: #fff; border-radius: 30rpx; color: #666; font-size: 26rpx; }
.type.active { background: #e53935; color: #fff; }
.row { background: #fff; margin: 12rpx 20rpx; padding: 24rpx; border-radius: 12rpx; display: flex; justify-content: space-between; align-items: center; }
.title { font-size: 28rpx; color: #333; }
.tag { font-size: 22rpx; color: #999; }
.empty { text-align: center; color: #999; padding: 80rpx 0; }
</style>
```

- [ ] **Step 3: 写 detail 页**

`lottery_frontend/src/pages/guide/detail.vue`:
```vue
<template>
  <view class="page">
    <view v-if="guide" class="card">
      <text class="title">{{ guide.title }}</text>
      <rich-text class="content" :nodes="guide.content"></rich-text>
    </view>
    <view v-else class="empty">{{ emptyMsg }}</view>
  </view>
</template>

<script setup>
import { ref } from 'vue'
import { onLoad } from '@dcloudio/uni-app'
import { getGuideDetail } from '../../api/guide.js'

const guide = ref(null)
const emptyMsg = ref('加载中…')

onLoad(async (q) => {
  try {
    guide.value = await getGuideDetail(q.id)
  } catch (e) {
    emptyMsg.value = e.msg || '加载失败'
    uni.showToast({ title: e.msg || '加载失败', icon: 'none' })
  }
})
</script>

<style scoped>
.card { background: #fff; margin: 20rpx; padding: 30rpx; border-radius: 16rpx; }
.title { font-size: 32rpx; font-weight: 600; color: #333; display: block; margin-bottom: 20rpx; }
.content { font-size: 28rpx; color: #555; line-height: 1.6; }
.empty { text-align: center; color: #999; padding: 80rpx 0; }
</style>
```

- [ ] **Step 4: 端到端目测**

后端：`lottery_backend` 激活 venv → seed_lotteries → seed_sample_guides → `python manage.py runserver 127.0.0.1:8123`。
前端：`lottery_frontend` 下先 `npm run build:h5`(确认 8 页全量编译通过)，再 `npm run dev:h5`。
Expected: 底部多出"玩法"tab；进入显示双色球玩法说明/奖级规则等条目；切类型/彩种刷新；点条目进详情看富文本。
curl 验证：`curl "http://127.0.0.1:8123/api/openapi/guide/list?code=ssq"` 返回条目；`curl "http://127.0.0.1:8123/api/openapi/guide/detail?id=1"` 含 content。
完成后停掉后台进程。

- [ ] **Step 5: 全量前端测试**

Run: `npm test`
Expected: PASS（页面不影响既有单测，全绿）

- [ ] **Step 6: 提交**

```bash
git add lottery_frontend/src/pages/guide/index.vue lottery_frontend/src/pages/guide/detail.vue lottery_frontend/src/pages.json
git commit -m "feat: 玩法介绍页(列表+详情) + tabBar 玩法"
```

---

## 计划自检

**1. Spec 覆盖：**
- PlayGuide 模型(lottery 可空/type/content/sort/is_active/publish_at) → Task 1 ✓
- Django Admin 内容管理 → Task 1 PlayGuideAdmin ✓
- guide/list(code 含通用 + type + is_active + publish_at 过滤, 不含 content) → Task 2 ✓
- guide/detail(含 content, 非法/未发布 code=1) → Task 2 ✓
- seed_sample_guides → Task 3 ✓
- 前端 guide api → Task 4 ✓
- 玩法页(彩种+类型切换+列表+详情 rich-text) + tabBar 玩法 → Task 5 ✓
- 合规(无预测/推荐) → 各 content/title 文案 ✓

**2. Placeholder 扫描：** 无 TBD/TODO；每步含完整代码。

**3. 类型/签名一致性：**
- `PlayGuide.TYPE_INTRO/RULE/ACTIVITY`(Task1) 被 seed(Task3)/测试引用一致。
- `GuideListSerializer`(不含 content)/`GuideDetailSerializer`(含 content)(Task2) 与前端列表/详情取法一致(列表无 content、详情有)。
- `type_label` 由 get_type_display 提供(Task2)，前端 index 直接展示。
- `getGuideList(code,type)`/`getGuideDetail(id)`(Task4) 被 Task5 页面引用一致。
- detail id 先 int() 兜底(Task2) 防 500。

**4. 注意点（给执行者）：**
- config/urls 的 `api/openapi/guide/` include 必须在 `api/openapi/`(lottery) include 之前，避免前缀被抢。
- content 存 HTML，前端 `<rich-text :nodes>` 渲染；seed 的 content 是 HTML 片段。
- tabBar 5 项是 uni-app 上限，正好；顺序 当期/历史/统计/玩法/我的。
- 端到端目测需后端 8123 + 前端 5199(vite 已绑)；先 seed_sample_guides 才有内容。
- 合规：content/title 不得含"预测/推荐"。
